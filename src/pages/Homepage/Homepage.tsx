import { Layout } from '../../common';
import CardList from './components/CardList/CardList';

const Homepage = () => {
  return (
    <Layout>
      <CardList />
    </Layout>
  );
};

export default Homepage;
